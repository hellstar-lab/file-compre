# Render environment variables for the File Compressor (Docker service)
# Paste these into Render -> Environment -> Add Environment Variable (one per line).

# Bind the Python HTTP server to all interfaces so Render can route traffic
HOST=0.0.0.0

# Match the exposed container port; Render will connect to this
PORT=8080